# Copy to `.env` or `server/.env` and fill in real secrets
NEON_DATABASE_URL=postgresql://<username>:<password>@<host>/<database>?sslmode=require
VITE_API_URL=http://localhost:4000

# Example server port
PORT=4000
